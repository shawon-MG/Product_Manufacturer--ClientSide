import React from 'react';
import Loading from '../Shared/Loading';

const About = () => {
    return (
        <div>
            <h1> About </h1>
            <Loading />
        </div>
    );
};

export default About;